# Phase 0 계산 엔진 저장소

`자동매매_시스템_기획안_v4.md` STAGE 7의 즉시 실행계획 중 **A그룹(코드/구조 작업)**을
패키지 형태로 구현한 것. 원본 `phase0_engine.py`(단일 파일 스크립트)를 그대로
유지하되, CI로 자동 검증되는 패키지 구조로 재편했다.

## 구조와 기획안 대응관계

| 경로 | 기획안 근거 | 내용 |
|---|---|---|
| `phase0/engine/core.py` | STAGE 3 핵심 수식 | 원본 수식 그대로. 하드코딩 없음 |
| `phase0/engine/tables.py` | STAGE 3 표1~8 | 표 생성 — costs.yaml에서 비용을 주입받음 |
| `phase0/config/costs.yaml` | §5.5 비용 구성 분해 | 태그별(확인된 사실/외부 확인 필요 등) 비용 파라미터 |
| `phase0/config/costs.py` | §5.5 "하드코딩 금지" | costs.yaml 로더 + 시나리오별 비용 계산 |
| `phase0/bootstrap/cluster_bootstrap.py` | §5.7 (R-08) | 거래일 클러스터 통합 부트스트랩 |
| `phase0/backtest/g0_backtester.py` | §5.4 (R-05), STAGE 4 G0 | 낙관/보수 이중 경로 + 4분기 판정 |
| `phase0/data/pykrx_ingest.py` | §5.8 (R-09), STAGE 7 항목 6·7 | KRX 실데이터 수집·클렌징 (pykrx) |
| `phase0/data/candidate_batch.py` | STAGE 7 항목 4 (v3 T-03) | 후보 종목 배치 수집·유동성 요약 |
| `phase0/strategy/vcb_gap.py` | STAGE 7 항목 9·10 | 첫 규칙 기반 전략(VCB-Gap, fable5 설계) |
| `phase0/strategy/gap_rebound.py` | STAGE 7 항목 9·10 | 두 번째 전략(GDR, 평균회귀형, fable5 설계) |
| `phase0/strategy/gap_rebound_v.py` | STAGE 7 항목 9·10 | GDR + 거래량 확인(C6) 변형(GDR-V, fable 설계) |
| `phase0/strategy/vbp.py` | STAGE 7 항목 9·10 | 세 번째 전략(VBP, 거래량 앵커 눌림목, fable 설계) |
| `phase0/engine/position_sizing.py` | AlgoLab 리서치 반영 | 리스크 기반 포지션 사이징(1~2% 룰) |
| `phase0/risk/circuit_breaker.py` | AlgoLab 리서치 반영 | 손실 대응 서킷브레이커(사전 커밋 정책) |
| `phase0/ml/gdr_filter.py` | 2026-07-15, ML 스마트 필터 | GDR 신호 특징 추출 (로지스틱 회귀 입력) |
| `phase0/paper/trade_log.py` | 2026-07-15, 페이퍼 트레이딩 | 실주문 없이 신호·가상 체결 기록(JSONL) |
| `tests/` | STAGE 3 "단위 테스트 통과 없이는 표 출력 금지" | 항등식 5·경계값 3 + 문서 수치 재현 72건 |
| `.github/workflows/ci.yml` | 위 원칙의 CI 게이트 | 매 push/PR마다 자동 실행 |

## 실행

```bash
pip install -e ".[dev,data]"
pytest tests/ -v --cov=phase0                       # 전체 테스트 (130건, 네트워크 불필요)
python scripts/print_tables.py                       # 문서 STAGE 3 표1~8 재출력
python scripts/ingest_real_sample.py 005930 20260701 20260714  # 실제 KRX 데이터 수집 확인
python scripts/run_candidate_batch.py 20260601 20260714         # 후보 종목 배치 수집(기본 5종목)
```

## 원본과 다른 점 (의도적 변경, 수치 아님)

- `t_annual()`은 이제 하드코딩된 4개 시나리오만 받지만, 원본과 완전히 동일한
  출력을 낸다 — 구조만 재편.
- **미세 발견 1건**: 원본 `t_cost_sens`는 Conservative 비용을 `.0047`로
  하드코딩했는데, 이는 `Base(0.36%) × 1.3 = 0.468%`를 표시 단계에서 반올림한
  값(0.47%)을 계산에 재사용한 것이다. 반올림 없이 그대로 계산하면 `E_trade =
  -0.048%`이며, 원본 표기(`-0.050%`)와 0.002%p 차이가 난다. 판정 방향에는
  영향 없음(둘 다 음수 → Conservative 미통과는 동일)이지만, 이 자체가
  기획안이 R-12에서 지적한 "표시값을 다시 계산에 흘려 넣는" 패턴의 축소판이라
  일부러 남겨두고 테스트로 고정했다(`tests/test_tables.py`).

## Phase 0 Blocker 해소 (§5.6) — 2026-07-14

`costs.yaml`의 `외부 확인 필요` 항목 2건이 모두 실측값으로 대체되어 Blocker가
해소됐다. Base 왕복비용은 **0.36%(문서 원본 가정치) → 0.3854%(실측)** 로 갱신:

- `brokerage_commission_roundtrip` — 사용자 실계좌(한국투자증권 뱅키스 온라인매매)
  확인 결과 0.0140527%(편도, KRX). 왕복 2배 적용 = 0.0281054%. 기존 0.01%
  임시값 대비 약 2.8배 상향.
- `exchange_fees` — KIS 공식 수수료안내 페이지 기준 유관기관제비용율
  0.00363960%(편도, 2025-10-27 고시). 왕복 추정 0.0073%로 Base에 반영.

이 갱신에 따라 `t_r01_verify`처럼 하드코딩된 예시(원표 조건 C=0.3% 고정)를
제외한 모든 표·테스트가 새 Base(0.3854%) 기준으로 자동 재계산되며,
`tests/test_costs.py`·`tests/test_tables.py`의 관련 assertion도 이 커밋에서
함께 갱신했다. 앞으로 비용이 또 바뀌면 `costs.yaml`의 `value`만 고치고
`pytest`를 돌려 실패하는 assertion만 새 값으로 맞추면 된다.

## Project Blocker 해소 (§5.6) — 2026-07-14

KIS 약관상 자동매매(Open API 기반 개인 알고리즘 거래) 금지 조항 여부를 사용자가
직접 확인 — **금지 조항 없음**. 조사 과정에서 함께 확인된 정황: KRX 알고리즘거래
사전신고 의무는 개인이 아닌 회원(증권사) 대상이며 파생상품계좌 한정 조항이라
이 프로젝트(KOSPI 주식 데이트레이딩)와는 무관, "프로그램매매" 규제(사이드카 등)는
동시 5억원 이상 대량 주문의 기관·외국인 대상이라 개인 알고리즘 거래는 해당 없음,
업계 전반(KIS 2022~, 토스증권 2026~)이 개인용 오픈API 자동매매를 활발히
마케팅하는 정착된 상품 카테고리. 단, 이용약관 원문 전체를 읽고 확정한 법률
자문은 아니며 사용자 본인 확인에 근거한다.

## pykrx 실데이터 연결 (STAGE 7 항목 6·7) — 2026-07-14, 부분 완료

`phase0/data/pykrx_ingest.py`로 실제 KRX 일봉 데이터 수집·클렌징 파이프라인을
연결했다. `scripts/ingest_real_sample.py`로 실제 네트워크 호출을 확인함:

- **개별 종목 시계열(`get_market_ohlcv`)은 정상 동작** — 삼성전자(005930)
  2026-07-01~14 10봉을 실제로 수집·클렌징 성공.
- **전체 시장 스냅샷(`get_market_ticker_list`, `get_market_cap`)은 현재
  깨져 있음** — KRX 서버 응답 형식이 바뀌어 `Expecting value: line 1 column 1`
  JSON 파싱 오류가 남. pykrx 저장소에 동일 증상의 미해결 이슈가 여러 건
  존재한다(sharebook-kr/pykrx#190, #191, #193) — 이 프로젝트 코드의 결함이
  아니라 pykrx·KRX 쪽 상위 장애다.
- **실질적 영향**: 항목 6(10년 일봉·시가총액·거래대금 수집)은 종목을 이미
  아는 경우 가능하지만, 항목 7(상장폐지 종목 포함 여부, 생존편향 방지)에
  필요한 "과거 시점 전체 유니버스 목록"은 이 상위 장애가 풀리기 전까지 이
  경로만으로는 완결할 수 없다. `fetch_universe()`는 이 실패를 조용히 삼키지
  않고 `UniverseUnavailableError`로 명시적으로 알린다.
- 클렌징 로직(휴장일 0봉 제거, OHLC 정합성 검증)과 유니버스 실패 처리는
  fetcher를 주입받는 구조라 네트워크 없이 pytest로 검증된다
  (`tests/test_pykrx_ingest.py`, 9건).

**다음 단계**: pykrx 업스트림 수정을 기다리거나, KRX 정보데이터시스템 등
대체 소스로 과거 유니버스(상장폐지 포함)를 구해야 항목 7이 완결된다.

## 후보 종목 배치 수집 (STAGE 7 항목 4, v3 T-03) — 2026-07-14

`phase0/data/candidate_batch.py`: 후보 리스트 → 종목별 일봉 수집 → 유동성
요약(평균 일 거래대금 프록시 = mean(종가×거래량))까지 배치로 처리한다.
`scripts/run_candidate_batch.py`로 실제 KRX 데이터 5종목에 대해 실행 확인
(2026-06-01~07-14, 5/5 성공, 유동성 필터링까지 정상 동작).

- 종목 단위로 실패를 격리 — 한 종목이 상장폐지·네트워크 오류로 실패해도
  나머지 배치는 계속 진행하고, 실패 사유를 `TickerSummary.error`에 남긴다.
- `DEFAULT_CANDIDATES`는 **손으로 고른 대형·고유동성 KOSPI 종목 20개
  예시(플레이스홀더)**다 — 항목 7(전체 유니버스 스냅샷)이 막혀 있는 상태에서
  파이프라인 자체를 실데이터로 검증하기 위한 목록일 뿐, 실전 후보 선정
  근거가 아니다. 실제 후보 50종목은 항목 7이 풀리거나 사용자가 직접
  확정해야 한다.
- 테스트(`tests/test_candidate_batch.py`, 5건)는 fetcher를 주입해 네트워크
  없이 배치 격리·필터링 로직만 검증한다.

## 분봉 이력 범위 (STAGE 7 항목 5, 분봉 검증 Blocker) — 2026-07-14, **실계좌로 확정**

`scripts/check_minute_bar_history.py`로 사용자 본인 KIS 계좌(모의투자,
APP Key/Secret 발급 후 `.env`에 설정)로 실제 호출해 확정: 삼성전자(005930)
분봉조회(`FHKST03010200`) 응답 30봉의 날짜가 전부 `20260714`(당일) 하나
뿐이었다 — 앞서 커뮤니티 자료로 냈던 "당일만 가능(이력 자체가 없음)"
잠정 결론이 실계좌로도 그대로 확인됨. **Blocker 해소.**

**설계에 미치는 영향(아직 코드에 반영 안 함)**: `g0_backtester.py`는 G0
"부호 갈림"(낙관>0, 보수≤0) 케이스를 "분봉 표본으로 재판정 1회"로 풀도록
설계돼 있는데, 분봉 이력이 없으므로 그 재판정은 과거 데이터로 소급할 수
없고 해당 상황이 다시 발생할 때 실시간으로만 관찰 가능하다 — "과거를 캐서
검증"이 아니라 "앞으로 관찰해서 검증"으로 설계를 바꿔야 한다. 아직 코드에는
반영하지 않았다(사용자 확인 대기).

## 첫 전략 VCB-Gap + G0 실데이터 백테스트 (STAGE 7 항목 9·10) — 2026-07-15

Phase 0 문서는 계산 엔진·Gate 구조만 다루고 전략 자체는 정의하지 않았다.
사용자가 "다른 사람 방식(딥러닝+100여 전략)을 바로 차용하지 말고, 규칙
기반 전략 하나부터 G0를 통과시켜보자"고 결정 → fable5(claude-fable-5)에게
설계를 위임해 받은 첫 전략을 실제 10년치 KRX 데이터로 검증했다.

**전략(`phase0/strategy/vcb_gap.py`, "VCB-Gap")**: 모멘텀 지속형. 전일이
20일 신고가를 거래량 1.5배 이상 동반해 돌파하며 강하게(CLV≥0.70) 마감했고,
오늘 시가 갭이 −0.5%~+2.0%(모멘텀을 부정하지도, 소진하지도 않은 범위)이며
ATR%가 1.0~5.0%(너무 조용하지도 과열되지도 않음)일 때 시가에 진입. 목표가/
손절가는 ATR% 배수(k_target/k_stop). D-1까지의 일봉 + 오늘 시가만 사용해
분봉 이력 없이도(위 §분봉 이력 범위 참고) 백테스트 가능하도록 설계됨.

**실행 결과** (`scripts/run_g0_backtest.py`, DEFAULT_CANDIDATES 20종목,
2016-07~2026-07 실KRX 데이터, 사전 등록 격자 6개 조합 전부):

| k_target | k_stop | 신호수 | E_conservative | E_optimistic | 판정 |
|---|---|---|---|---|---|
| 1.2 | 0.75 | 685 | −0.638% | −0.633% | insufficient_sample |
| 1.2 | 1.00 | 685 | −0.608% | −0.613% | insufficient_sample |
| 1.5 | 0.75 | 685 | −0.619% | −0.619% | insufficient_sample |
| 1.5 | 1.00 | 685 | −0.590% | −0.597% | insufficient_sample |
| 2.0 | 0.75 | 685 | −0.602% | −0.598% | insufficient_sample |
| 2.0 | 1.00 | 685 | −0.577% | −0.577% | insufficient_sample |

**정직한 결론**: 6개 조합 전부 낙관·보수 경로 모두 마이너스로 일관됨.
표본(685건)이 G0 기준(1,000건)에 못 미쳐 형식적 판정은 "표본 부족"이지만,
부호가 모든 조합·양쪽 경로에서 한결같이 음수라 표본을 더 채운다고 뒤집힐
가능성은 낮아 보인다 — **이 진입 규칙 자체가 KOSPI 대형주·2016~2026
구간에서 비용을 넘는 엣지를 보여주지 못했다**는 것이 현재까지 가장 정직한
해석이다. fable5가 설계 시 명시한 "장세 의존성으로 인한 기각은 버그가
아니라 정당한 G0 기각"이 실제로 발생한 사례로 본다. 목표/손절 배수를
격자 밖에서 추가로 찾는 것은 데이터마이닝이므로 하지 않았다.

**다음으로 고려할 것(미결정, 사용자 판단 대기)**: (a) 진입 로직(C1~C6) 자체를
다른 가설로 다시 설계, (b) 기간을 나눠 장세별(추세장/박스권) 부분 표본을
따로 봐서 regime-dependence 확인, (c) 후보 종목을 늘려 표본 1,000건을
채운 뒤 재확인(부호가 이미 일관되게 음수라 우선순위는 낮음).

## 두 번째 전략 GDR(갭하락 반등) — 2026-07-15

사용자가 "아예 다른 가설로 가보자"고 결정 → fable5에게 VCB-Gap의 실패
진단(당일청산 제약이 '지속형' 가설과 구조적으로 안 맞았을 가능성)을
그대로 전달하고 재설계 요청. fable5가 제시한 논거: VCB-Gap의 실측
E_trade(−0.58~−0.64%)가 '엣지 없음' 중립 기준선(−0.12%)보다도 나빴다는
것은 이 유니버스에 단기 반전(reversal) 성향이 실재한다는 방증이므로,
정확히 반대 부호(과매도 이탈 매수)를 조건화하면 그 반전을 포착할 수
있다는 것.

**전략(`phase0/strategy/gap_rebound.py`, "GDR — Gap-Down Rebound")**:
평균회귀형. 하락추세가 아닌 종목(종가≥0.98×SMA20)이 비정상적으로
갭하락(−1.2%~−4.5%, 종목 자체 ATR% 대비로도 비정상)했지만 전일 자체가
이미 급락(−5% 초과)한 건 아닐 때 시가에 매수, 목표가는 전일종가 방향
갭의 부분 되메움(f_fill), 손절은 ATR% 기반. 12월 배당락 구간(12/22~31)은
기계적 갭이라 제외.

**실행 결과** (동일 20종목, 2016-07~2026-07, 사전 등록 격자 6개 조합):

| f_fill | k_stop | 신호수 | E_conservative | E_optimistic | 판정 |
|---|---|---|---|---|---|
| 0.6 | 1.0 | 582 | −0.253% | −0.282% | insufficient_sample |
| 0.6 | 1.5 | 582 | −0.288% | −0.292% | insufficient_sample |
| 0.8 | 1.0 | 582 | **−0.187%** | −0.201% | insufficient_sample |
| 0.8 | 1.5 | 582 | −0.244% | −0.247% | insufficient_sample |
| 1.0 | 1.0 | 582 | −0.218% | −0.227% | insufficient_sample |
| 1.0 | 1.5 | 582 | −0.281% | −0.284% | insufficient_sample |

**정직한 결론**: 여전히 6개 조합 전부 마이너스라 판정을 뒤집지는 못했다.
다만 VCB-Gap과 성격이 다르다 — 최선 조합(f_fill=0.8, k_stop=1.0)의
−0.187%는 '엣지 없음' 중립 기준선(−C=−0.385%)**보다 낫다**. VCB-Gap은
조건화가 중립보다 손실을 키우는 방향으로 작동했는데(반대 신호), GDR은
조건화가 손실을 줄이는 방향으로 작동했다 — 평균회귀 가설이 부분적으로
지지된다는 뜻이지, 아직 비용을 완전히 넘어서지는 못한다는 뜻이다. 표본
582건도 G0 기준(1,000건) 미달. 격자를 더 넓히는 건 데이터마이닝이라
하지 않았다.

## 외부 리서치(AlgoLab 블로그) 반영 — 2026-07-15

algolab.co.kr 자동매매 가이드 블로그의 관련 글(변동성 돌파, 백테스트
함정, 리스크 관리, 검증 3단계, 멀티전략 포트폴리오 등 19편)을 조사해
우리 시스템과 대조했다. 새로 발견된 갭: (1) GDR에는 동종 평균회귀
전략(RSI+스토캐스틱)이 쓰는 거래량 확인 조건이 빠져 있었음, (2) 세
번째 가설 후보로 "거래량 동반 돌파"가 당일청산 환경에 적합하다는
제안 — 단, 원안의 "시간대 필터"는 우리 데이터 제약(KIS 분봉 이력
없음)상 문자 그대로 구현 불가라 다일(多日) 거래량 궤적으로 대체.
포지션 사이징 공식 부재, 손실 대응(서킷브레이커) 정책 미문서화 등은
아직 실행하지 않은 항목으로 남겨둠(코드 변경 없음).

## 세 번째·네 번째 실험: GDR-V·VBP — 2026-07-15

**GDR-V(`phase0/strategy/gap_rebound_v.py`)**: 기존 GDR의 C1~C5는
그대로 두고 C6(거래량 확인: D-1 거래량 ≥ v_mult × 전일 제외 20일
평균)을 AND로 추가한 별도 가설. 원 GDR 결과를 덮어쓰지 않고 새로
사전 등록(f_fill×k_stop 기존 6칸 × v_mult{1.2,1.5} = 12칸).
Campbell-Grossman-Wang류 거래량-반전 이론 근거: 전일 대량거래를
동반한 갭하락은 유동성발 오버슈트(평균회귀), 거래량 없는 갭하락은
정보발 재평가(지속)일 확률이 높다는 가설.

| f_fill | k_stop | v_mult | 신호수 | E_conservative | 판정 |
|---|---|---|---|---|---|
| 0.6 | 1.0 | 1.2 | 173 | −0.364% | insufficient_sample |
| 0.6 | 1.0 | 1.5 | 89 | −0.247% | insufficient_sample |
| 0.6 | 1.5 | 1.2 | 173 | −0.433% | insufficient_sample |
| 0.6 | 1.5 | 1.5 | 89 | −0.165% | insufficient_sample |
| 0.8 | 1.0 | 1.2 | 173 | −0.256% | insufficient_sample |
| 0.8 | 1.0 | 1.5 | 89 | −0.129% | insufficient_sample |
| 0.8 | 1.5 | 1.2 | 173 | −0.384% | insufficient_sample |
| 0.8 | 1.5 | 1.5 | 89 | −0.164% | insufficient_sample |
| 1.0 | 1.0 | 1.2 | 173 | −0.207% | insufficient_sample |
| 1.0 | 1.0 | 1.5 | 89 | **−0.016%** | insufficient_sample |
| 1.0 | 1.5 | 1.2 | 173 | −0.334% | insufficient_sample |
| 1.0 | 1.5 | 1.5 | 89 | −0.049% | insufficient_sample |

**정직한 결론**: 12칸 전부 여전히 마이너스. 다만 사전등록한 반증
가능 예측(v_mult이 클수록 나아야 한다)이 **6쌍 전부에서 예외 없이
성립**했다 — 순수 우연으로 보기 어려운 일관된 용량-반응 패턴이다.
최선 조합(f_fill=1.0, k_stop=1.0, v_mult=1.5)은 −0.016%로 0에 근접.
신호 수는 예상대로 GDR(582건) 대비 크게 줄었다(89~173건) — 표본
부족은 더 심해졌지만, 방향성 증거는 GDR보다 뚜렷해졌다.

**VBP(`phase0/strategy/vbp.py`, Volume-anchored Breakout Pullback)**:
VCB-Gap·GDR 둘 다와 구조적으로 다른 세 번째 가설. 대량거래 동반
돌파로 만들어진 레벨(앵커, D-2~D-4 중 가장 최근)을 거래량 수축 속에
1~3일 방어한 종목이 약세~보합 시가로 열리면, 그 시가를 사서 앵커 이후
스윙 고점(H_ref)으로의 부분 되돌림을 목표로 한다. 목표/손절 둘 다
전일 종가가 아니라 돌파 레벨(구조) 기준 — 손절되는 거래는 "레벨이
무너졌다"는 가설의 반증점과 정확히 일치하도록 설계. VCB-Gap 실패
원인(휩쏘, 강세 시가 매수)을 조건에서 직접 배제.

| f_ret | k_stop | 신호수 | E_conservative | 판정 |
|---|---|---|---|---|
| 0.50 | 0.75 | 121 | −0.021% | insufficient_sample |
| 0.50 | 1.25 | 121 | **+0.061%** | insufficient_sample |
| 0.75 | 0.75 | 121 | −0.140% | insufficient_sample |
| 0.75 | 1.25 | 121 | −0.081% | insufficient_sample |
| 1.00 | 0.75 | 121 | −0.146% | insufficient_sample |
| 1.00 | 1.25 | 121 | −0.087% | insufficient_sample |

**정직한 결론**: (f_ret=0.5, k_stop=1.25) 조합이 **이 프로젝트 전체를
통틀어 처음으로 비용(0.3854%) 포함 순수익 기대값이 양수**로 나왔다.
다만 신호 121건·거래일 112일로 G0 표본 기준(1,000건·500일)에 한참
못 미쳐 판정은 여전히 insufficient_sample이다 — "이길 것 같다"가
아니라 "아직 판단할 수 없다"가 정확한 해석이다. 6칸 중 1칸만
양수라는 점, 나머지 5칸은 −0.02%~−0.15%로 여전히 마이너스라는 점도
같이 봐야 한다 — 최선 칸 하나만 골라 보고하는 것은 데이터마이닝이므로
전수 결과를 그대로 남긴다.

**네 실험 종합**: VCB-Gap(−0.58~−0.64%, 중립보다 나쁨) → GDR(−0.19~
−0.29%, 중립보다 나음) → GDR-V(−0.02~−0.43%, 용량-반응 패턴 확인) →
VBP(−0.15~+0.06%, 최초 양수 칸 등장). 매 단계 조건화가 정직하게
손실을 줄이는 방향으로 진행되고 있다. 그러나 넷 다 표본 부족
(insufficient_sample)이라 어느 것도 아직 G0을 "통과"하지 못했다 —
다음으로 필요한 건 새 조건 추가가 아니라 **표본 확대**(유니버스
확장 또는 기간 연장)일 가능성이 높다.

## 유니버스 확장(20→95종목) — 2026-07-15, 표본 부족 문제에 대한 정직한 반전

네 실험 전부가 insufficient_sample에 막혀 있다는 진단에 따라, 표본을 늘리는
쪽을 우선했다. companiesmarketcap.com에서 시가총액 상위 KOSPI 종목을 조사한
뒤 `pykrx.stock.get_market_ticker_name()`으로 종목코드 하나하나를 실제
등록명과 대조 검증(코스닥·우선주·ADR 전용 코드 제외)해 75종목을 추가,
`phase0/data/candidate_batch.py`의 `EXPANDED_CANDIDATES`(95종목)로 확정.
`scripts/run_g0_backtest.py`에 `--universe expanded`와 `--strategy all`
(종목 데이터를 한 번만 수집해 네 전략 전부를 그 위에서 돎)을 추가했다.

**결과 (95종목, 2016-07~2026-07, 네 전략 전부 재실행):**

| 전략 | 신호수 | 거래일 | 최선 E_cons | 판정 |
|---|---|---|---|---|
| VCB-Gap | 2,803 | 1,320 | −0.667% | **reject** (표본기준 통과, 최초 확정 판정) |
| GDR | 1,595 | 558 | −0.160% | **reject** (표본기준 통과, 최초 확정 판정) |
| GDR-V | 288~489 | 178~264 | −0.168% | insufficient_sample |
| VBP | 532 | 412 | −0.115% | insufficient_sample |

**정직한 결론 — 두 가지**:

1. VCB-Gap과 GDR은 이제 표본 기준(1,000건·500일)을 넘어 **G0 최초의
   확정 판정("reject")**을 받았다. 20종목 결과와 방향이 일관된다 —
   VCB-Gap이 훨씬 나쁘고(−0.67%), GDR이 비용에 가장 가깝다(−0.16%).
   추측이 아니라 이제는 확정된 사실이다.

2. **GDR-V·VBP에서 봤던 "유망해 보이던" 패턴 두 개가 재현에 실패했다.**
   - GDR-V: 20종목에서는 "v_mult가 클수록 낫다"는 용량-반응 예측이 6쌍
     전부에서 성립했으나, 95종목에서는 **6쌍 전부 정반대로 뒤집혔다**
     (v_mult=1.2가 1.5보다 나음). 우연으로 보기 어려웠던 패턴이 표본을
     늘리자 정반대가 됐다는 것은, 애초 패턴이 인과효과가 아니라 20종목
     표본의 노이즈였다는 뜻이다.
   - VBP: 20종목에서 유일하게 양수였던 칸(f_ret=0.5, k_stop=1.25,
     +0.061%)이 95종목에서는 **−0.1147%로 반전**됐다.

이것은 실패가 아니라 이 프로젝트가 원래 하려던 일이 정확히 작동한
사례다 — 작은 표본에서 나온 그럴듯한 패턴을 성급하게 채택하지 않고
표본을 늘려 재검증했더니, 그 패턴들이 노이즈였음이 드러났다. 데이터
마이닝 방지 원칙(사전등록, 전수 보고)이 없었다면 GDR-V나 VBP의 "유망한
칸"을 근거로 다음 단계로 넘어갔을 것이고, 그건 잘못된 결론이었을
것이다.

**남은 것**: GDR이 넷 중 비용에 가장 가깝다(−0.16%)는 사실은 95종목에서도
유지됐다.

## GDR 부트스트랩 신뢰구간 (`scripts/run_gdr_bootstrap.py`) — 2026-07-15

GDR은 이미 표본 기준을 넘겨 확정 기각을 받았으므로, 종목을 더 추가해
부호가 바뀌길 기대하는 것은 GDR-V/VBP에서 막 확인한 것과 같은 재현성
함정이 될 수 있다고 판단했다. 대신 −0.160%라는 점추정치가 통계적으로
0(손익분기)과 구분되는지를 정량화하기로 했다 — `phase0/bootstrap/
cluster_bootstrap.py`(이동 블록 부트스트랩, §5.7)를 실거래 데이터에
처음으로 배선했다(그동안 합성 데이터로만 검증돼 있었음).

방법: 95종목 GDR 신호를 종목별로 conservative 경로 해소한 뒤, 거래를
날짜별로 묶어(같은 날 여러 종목 거래를 하나의 날짜 블록으로 유지 —
§5.7의 핵심 요구사항) 블록 길이 10/15/20일 세 가지로 각각 1,000회
재표본, gross E_trade에서 비용(0.3854%)을 뺀 net E_trade의 95% 신뢰구간을
산출했다. (계좌 지표 CAGR/MDD는 포지션 사이징 모델이 없어 산출하지
않았다 — 거래 단위 지표만 유효하다.)

**결과**: 사전 등록 6개 조합 × 블록길이 3가지 = 18개 전부 95% 신뢰구간이
**0을 완전히 배제**(전부 음수 구간, 예: f_fill=1.0/k_stop=1.0/block=15 →
점추정 −0.176%, [−0.336%, −0.003%]). 블록 길이를 10/15/20으로 바꿔도
결론이 흔들리지 않았다.

**정직한 결론**: GDR의 기각은 이제 "표본이 적어 아직 모른다"가 아니라
"표본이 충분하고, 신뢰구간이 통계적으로 0을 배제한다"로 격상됐다 —
넷 중 비용에 가장 가까웠던 가설이지만, 이 유니버스·이 비용 구조에서는
통계적으로 유의하게 손실이라는 뜻이다. 데이터마이닝 없이 표본을
늘리고 신뢰구간까지 확인한 지금, VCB-Gap/GDR/GDR-V/VBP 네 가설 모두
G0을 통과하지 못했다는 게 이 저장소가 실제 KRX 데이터로 도달한 현재까지의
최종 결론이다.

## 슬리피지 가정 스팟체크 (`scripts/check_orderbook_spread.py`) — 2026-07-15

GDR이 유일하게 비용에 근접했던(−0.16%, 통계적으로도 확정 기각) 가설이므로,
새 가설을 더 만들기보다 costs.yaml의 slippage_buy+slippage_sell_normal
= 0.15%("초기 가정", 섀도우 테스트 후 확정)가 과대평가됐는지를 실측하는
게 근거가 더 확실한 다음 수라고 판단했다. 사용자 실계좌(KIS) 인증으로
실시간 호가창(최우선 매수/매도호가)을 조회해 스프레드를 가정치와 비교하는
스크립트를 만들었다.

**실행 결과 1차(2026-07-15 00시경, 장 마감 후)**: 8개 대형주 전부 호가 없음
(장 시간 09:00~15:30 KST 외 — 당연한 결과). 인증·토큰 발급·API 호출
경로는 전부 정상 동작 확인됨.

**실행 결과 2차(2026-07-15 09시경, 장 시작 직후)**:

| 종목 | 매수호가1 | 매도호가1 | 스프레드 |
|---|---|---|---|
| 삼성전자 | 279,000 | 279,500 | 0.179% |
| SK하이닉스 | 2,089,000 | 2,091,000 | 0.096% |
| 현대차 | 433,500 | 434,000 | 0.115% |
| 기아 | 143,500 | 143,700 | 0.139% |
| NAVER | 190,200 | 190,300 | 0.053% |
| KB금융 | 184,900 | 185,000 | 0.054% |
| LG화학 | 265,000 | 265,500 | 0.189% |
| 삼성생명 | 338,000 | 339,000 | 0.295% |

평균 스프레드 0.140% vs 가정된 왕복 슬리피지 0.150% — 비슷하지만 살짝
더 좁다. 다만 종목별 편차가 크다: NAVER·KB금융은 0.05%대로 가정보다
훨씬 좁은 반면 삼성생명은 0.295%로 가정의 거의 2배다. "가정이 명백히
과대평가"라고 단정하기엔 애매한, 종목마다 다른 그림이다.

**정직한 한계**: 이건 완전한 섀도우 테스트가 아니라 1회 스팟체크다(그것도
장 시작 직후이지 정확히 09:00:00 시가 순간은 아니다 — 실제 GDR 신호는
시가 그 자체에서 발생하는데 관측은 몇 분 뒤였다). costs.yaml의 태그를
"확인된 사실"로 바꾸려면 여러 날에 걸쳐, 특히 개장 직후 순간의 스프레드를
반복 관측해야 통계적으로 의미가 있다 — 오늘 하루 두 번(장중/장외) 관측만으로는
부족하고, slippage_buy/slippage_sell_normal 태그는 여전히 "초기 가정"으로
남겨둔다.

## 포지션 사이징 + 손실 대응 정책 (AlgoLab 블로그 리서치 반영) — 2026-07-15

**`phase0/engine/position_sizing.py`**: `exposure()`가 슬롯당 고정 비중만
다뤄 손절 거리를 반영하지 않는다는 갭을 메운다. 표준 1~2% 룰(포지션 크기
= 자본 × 리스크% / (진입가−손절가))을 `g0_backtester.Signal` 계약
위에 그대로 얹었다 — `size_by_risk()`, 자본이 너무 작아 1주도 못 사면
예외 대신 `shares=0`을 반환(유효한 결과로 취급). 동시 보유 포지션 합산
리스크가 계좌 한도(기본 5%) 이내인지 확인하는 `portfolio_risk_ok()`도
추가.

**`phase0/risk/circuit_breaker.py`**: "결정은 손실을 보기 전에 고요할 때
정하고, 폭풍 속에서는 실행만 한다"는 원칙을 코드로 못박았다. 일일
−2%/주간 −5%/월간 −10% 손실, 연속손실 8회(예시값), 시장 급락 후 48시간
냉각 — 우선순위(냉각기간 > 월간 > 주간 > 일간 > 연속손실)로 판정하는
순수 함수 `check_halt()`. 아직 실거래 계좌 상태 추적과는 배선되지 않은
정책 뼈대다 — 실거래 배선은 이후 단계.

## GDR ML 스마트 필터 (`scripts/train_gdr_ml_filter.py`) — 2026-07-15

규칙 기반 가설 4개가 전부 실패한 뒤, 완전히 새 신호 로직 대신 이미 만든
GDR(95종목 격자 최선 조합 f_fill=1.0/k_stop=1.0 — ML 이전에 이미 확정된
선택) 신호 위에서 "이 신호가 이길 확률"을 로지스틱 회귀로 추정해 승률
낮은 신호를 거르는 스마트 필터를 시도했다. 사용자가 표본 크기(1,595건)
대비 딥러닝은 과적합 위험이 크다고 판단해 클래식 ML(로지스틱 회귀)로
시작하기로 결정.

**과적합 방지 설계(전부 결과 확인 전 고정)**: 12개 특징(gap%, ATR%, CLV,
SMA20 괴리, 전일수익률, 거래량비, 5/10/20일 수익률, RSI14, 요일, 월) —
전부 D-1까지 정보로만 계산. 하이퍼파라미터(C=1.0, class_weight=balanced)
그리드서치 없이 고정. 학습/평가는 날짜 기준 시간순 80%/20% 분할(2024-06-27
이후가 OOS) — 미래로 과거를 학습하지 않음. 필터 임계값(P(win)≥0.5)도
사전 고정, 평가는 단 한 번만.

**결과**:

| | 신호수 | 승률 | E_trade(OOS) |
|---|---|---|---|
| 필터 없음(원 GDR) | 373 | 51.47% | −0.4260% |
| ML 필터(P(win)≥0.5) | 184 | 49.46% | −0.4797% |

**정직한 결론 — 두 가지**:

1. **ML 필터는 도움이 안 됐다.** 신호를 절반으로 줄였는데도 승률이
   오히려 떨어졌다(51.47%→49.46%)는 것은 이 12개 특징으로는 GDR 신호의
   승패를 유의미하게 구분하지 못한다는 뜻이다 — "학습 구간 승률
   56.46%"라는 IS 성능이 OOS에서 재현되지 않은, 전형적인 과적합
   징후에 가깝다.
2. **더 중요한 발견**: 필터와 무관하게, OOS 구간(2024-06-27~2026-07)의
   원 GDR E_trade(−0.426%)가 10년 전체 평균(−0.160%)보다 뚜렷이
   나쁘다. 이는 GDR의(이미 작았던) 엣지가 최근 구간에서 더 나빠졌다는
   뜻 — AlgoLab 블로그가 경고했던 "시장 체제 변화(regime change)"가
   실제로 이 유니버스·이 기간에 나타나고 있을 가능성을 시사한다.

이로써 규칙 기반 4개 + ML 필터 1개, 총 5개 접근법이 전부 이 틀(KOSPI
대형주·당일청산·일봉 기반 신호) 안에서 실패했다. 데이터마이닝 없이
정직하게 도달한 현재 결론이다.

## 메타 감사: 데이터 스누핑/다중검정 관점에서 이 프로젝트 재점검 — 2026-07-15

AlgoLab 블로그에 2026-07-15자로 올라온 글("백테스트가 화려할수록 왜
의심해야 할까" — data-snooping/multiple-testing 함정)을 읽고, 지금까지
이 저장소에서 실제로 한 작업을 그 틀로 재점검했다. 핵심 주장: "얼마나
좋은가"가 아니라 "몇 번 시도했는가"를 알아야 결과를 해석할 수 있다 —
100개를 시험하면 실력 없이도 약 5개는 우연히 좋아 보인다.

**실제 시도 횟수 집계**: VCB-Gap(6조합) + GDR(6) + GDR-V(12) + VBP(6) +
ML 필터(1모델) = 파라미터 조합 기준 31개, 가설 계열 기준 5개. 블로그의
경험칙("100개 시험 → 약 5개 가짜 승자")을 대입하면, 31개 규모에서 우연히
좋아 보이는 결과가 1~2개 나오는 건 통계적으로 자연스러운 일이다 —
GDR-V의 "용량-반응 패턴"과 VBP의 "양수 칸" 두 건이 정확히 이 정도
규모다. 유니버스를 20→95종목으로 확장해 재현 여부를 확인한 것이 바로
이 함정에 대한 방어였고, 실제로 둘 다 재현에 실패해 노이즈로 판명났다
— 사후에 보니 이 블로그가 설명하는 메커니즘과 정확히 일치한다.

**그러나 이번 점검으로 드러난 실제 갭**: 블로그의 6가지 방어 규율 중
1번("탐색용 인샘플과 한 번도 안 본 아웃오브샘플 분리")을 우리는 ML
필터(80/20 시간순 분할)에만 적용했다. VCB-Gap·GDR·GDR-V·VBP 네 규칙
기반 전략은 전부 **10년 전체 데이터로 판정**했다 — 처음부터 떼어둔
진짜 홀드아웃이 없었다. 즉 같은 2016~2026 KOSPI 데이터셋에 다섯 개
가설 계열을 순차적으로 물어본 것 자체가, 개별 실험 내부의 규율(사전
등록·전수보고)과 별개로 메타 수준의 다중검정 노출이라는 뜻이다. 이건
소급 적용이 불가능하다 — 이미 이 데이터셋을 다섯 번 들여다봤으므로,
지금부터 홀드아웃을 새로 지정해도 "한 번도 안 본" 구간이 아니다.

**정직한 결론**: 이 10년치 데이터셋 위에서 여섯 번째 가설을 또 시도하는
행위는 근거가 점점 약해진다 — 개별 실험은 깨끗해도 데이터셋 자체가
점점 "탐색용으로 소진"되고 있기 때문이다. 이 데이터셋에서 전략을 계속
찾는 것보다, (a) 앞으로 실제로 쌓일 새 데이터(라이브/페이퍼 트레이딩
기간)를 진짜 한 번도 안 본 홀드아웃으로 남겨두고 시작하거나, (b) 전략
탐색 자체를 여기서 접고 인프라(옵션 3)로 넘어가는 쪽이 이 블로그의
방어 규율과 부합한다. 데이터마이닝 방지 원칙이 그동안 개별 실험
안에서는 잘 지켜졌지만, 이번 점검으로 그 원칙을 프로젝트 전체 수준
으로도 적용해야 한다는 게 분명해졌다.

## GDR 페이퍼 트레이딩 인프라 (`scripts/paper_trade_gdr.py`) — 2026-07-15

메타 감사에서 확인된 갭(진짜 홀드아웃 부재)을 메우는 첫 단계 — 이 10년치
데이터셋에 여섯 번째 가설을 또 물어보는 대신, **앞으로 실제로 쌓일 새
데이터를 진짜 한 번도 안 본 홀드아웃으로 축적**하기 시작한다. VPS 24시간
운영·텔레그램 감시 블로그 리서치를 반영해 인프라 골격을 만들었다(텔레그램
연동은 사용자가 아직 봇 토큰이 없어 이번엔 보류 — 로컬 로그·하트비트만
우선 구현).

**동작 방식**: 실주문은 절대 내지 않는다 — KIS 시세 조회(읽기 전용)만
쓴다. 장중 지속 폴링 없이 하루 두 번 실행으로 당일청산을 재현한다
(`g0_backtester.classify_bar`와 동일한 방식으로 장 마감 후 고가/저가/
종가만으로 resolve):

- `--mode entry` (평일 09:06 KST 자동 실행): 오늘 시가로 GDR 신호 생성
  (f_fill=1.0, k_stop=1.0 — 95종목 격자 챔피언 그대로 승계) → 서킷브레이커
  체크(`circuit_breaker.check_halt`) → 통과 시 `position_sizing.size_by_risk`
  로 포지션 크기 계산 → 로그에 미결 항목 기록.
- `--mode resolve` (평일 15:37 KST 자동 실행): 오늘 실제 고가/저가/종가로
  미결 항목을 해소, 승/패·pnl% 기록.

`phase0/paper/trade_log.py`: JSONL append-only 로그 + daily/weekly/
monthly_return·consecutive_losses 계산(서킷브레이커 입력용). **정직한
단순화**: 이 수익률들은 자본 복리·동시보유를 반영한 진짜 계좌 수익률이
아니라 해당 기간 청산 거래의 pnl% 단순평균이다 — 진짜 계좌 지표(CAGR/
MDD)가 필요하면 여전히 포지션 사이징이 반영된 실제 자본 곡선 모델이
따로 필요하다.

**로컬 스모크테스트(2026-07-15 19시경, 장 마감 후)**: 인증·시세조회·
하트비트 전부 정상 동작 확인(신호는 0건 — 장외 시간이라 당연). 로그는
`data/paper_trading/`에 쌓이며 개인 실행 기록이라 `.gitignore` 처리했다.

**주의**: 자동 실행 예약은 이 세션에 한정된 cron이라 세션이 끝나거나
7일이 지나면 만료된다 — 진짜 영속적인 24시간 운영을 원하면 VPS 배포
(리서치한 systemd `Restart=always` 패턴)가 다음 단계로 필요하다.

## 남은 것 (STAGE 7 B·C그룹 — 계좌·API 접근 필요)

- `cluster_bootstrap.py`는 여전히 합성 데이터로만 검증됐다 — 실전 배선은
  이후 단계.
- 분봉 이력 범위 확정 실호출은 이미 사용자 본인 계좌로 완료됨(위 참고).
