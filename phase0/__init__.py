"""Phase 0 계산 엔진 패키지.

원칙 (기획안 v4.0 STAGE 3 승계):
- 문서의 모든 계산 표는 이 패키지의 출력으로 생성한다. 수동 전사 금지.
- 단위 규약: 함수 인터페이스는 전부 '소수'(0.01 = 1%). 표시 계층에서만 %로 변환.
- 단위 테스트를 통과하지 못하면 표를 출력하지 않는다(engine.core.self_test 참조,
  CI에서는 tests/ 아래 pytest로 동일 항등식·경계값을 검증한다).
"""
