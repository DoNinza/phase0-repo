미국 정규장이 방금 마감됐다({{TIMEZONE}} 기준 화~토 05:13, KST — 현지
16:00 ET 직후). `{{REPO_DIR}}` 에서
`PYTHONIOENCODING=utf-8 python scripts/collect_minute_bars_us.py`를 실행해
20종목의 5분봉을 재수집하라(yfinance 60일 롤링 윈도우가 지나가기 전에
눌러 담는 것 — append_bars가 중복은 자동으로 걸러내므로 매번 전체
재수집해도 안전하다).

완료되면 종목별 응답 봉 개수를 짧게 확인하고, 실패한 종목이 있으면
알려라. 참고: 이 시각은 서머타임(DST) 전환 시 약 1시간 어긋날 수 있다 —
그런 낌새(예: 응답이 비정상적으로 적음)가 보이면 알려달라. 에러나
인증정보 문제가 있으면 반드시 알려라.
